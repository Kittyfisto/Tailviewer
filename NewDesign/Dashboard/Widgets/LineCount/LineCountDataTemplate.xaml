<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:NewDesign.Dashboard.Widgets.LineCount">
    
    <DataTemplate DataType="{x:Type local:LineCountViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <TextBlock Text="{Binding Count}" />
            <TextBlock Text="{Binding Caption}"
                       Grid.Row="1"/>
            
        </Grid>
    </DataTemplate>
    
</ResourceDictionary>