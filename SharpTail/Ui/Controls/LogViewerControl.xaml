<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SharpTail.Ui.Controls">

    <Style TargetType="{x:Type local:LogViewerControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:LogViewerControl}">

                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Filter:"
                                       Margin="8" />

                            <TextBox
                                Text="{Binding FilterString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Margin="8"
                                Grid.Column="1" />
                        </Grid>

                        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="8">
                            <TextBlock Text="Displaying " />
                            <TextBlock Text="{Binding LogEntryCount}" />
                            <TextBlock Text=" out of " />
                            <TextBlock Text="{Binding AllLogEntryCount}" />
                            <TextBlock Text=" entries" />
                        </StackPanel>

                        <ListBox
                            Grid.Row="2"
                            ItemsSource="{Binding LogEntries}"
                            VirtualizingPanel.IsVirtualizing="True"
                            ScrollViewer.VerticalScrollBarVisibility="Visible">

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                                >
                                        <!--<Border
                                            Background="LightBlue">
                                            <TextBlock
                                                Text="{Binding Index}"
                                                Margin="8,0,8,0" />
                                        </Border>-->
                                        <local:HighlightableTextBlock
                                            LogEntry="{Binding}"
                                            FilterString="{Binding FilterString}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>

                        </ListBox>

                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>